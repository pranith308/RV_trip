# Server-only. Do not put VITE_ in front of this name.
# Get a key from Google Cloud, enable Places API (New) and Routes API,
# restrict the key to those two APIs, and paste it here.
GOOGLE_MAPS_API_KEY=
